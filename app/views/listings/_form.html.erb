<section class="form">
  <div class="container">
    <div class="columns is-centered">
      <div class="column is-half">
        <%= form_with(model: listing, local: true) do |form| %>
          <% if listing.errors.any? %>
            <div id="error_explanation">
              <h2><%= pluralize(listing.errors.count, "error") %> prohibited this listing from being saved:</h2>

              <ul>
                <% listing.errors.full_messages.each do |message| %>
                  <li><%= message %></li>
                <% end %>
              </ul>
            </div>
          <% end %>

          <div class="field">
            <%= form.label :title %>
            <%= form.text_field :title, class:'input', placeholder:'Title' %>
          </div>

          <div class="field">
            <%= form.label :description %>
            <%= form.text_area :description, class:'input', placeholder:'Description', class:'textarea' %>
          </div>

          <div class="field">
            <%= form.label :price %>
            <%= form.text_field :price, class:'input', placeholder:'Price' %>
          </div>
          
          <div class="field">
            <%= form.label :category_id %>
            <%= form.select(:category_id, options_from_collection_for_select(@categories, :id, :name), class:'select') %>
          </div>

          <div class="field">
            <p class="control">
              <%= form.submit "Create Listing!", class:'button is-primary is-outlined' %>
            </p>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</section>
